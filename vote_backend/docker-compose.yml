version: '3'
 
services:
  vote_backend-service-node2:
    network_mode: host
    build: .
    depends_on:
      - redis
      - postgres
      

  redis:
    image: redis:latest
    ports:
      - "6379:6379" 

  postgres:
    image: postgres:latest
    ports:
      - "5432:5432"

  